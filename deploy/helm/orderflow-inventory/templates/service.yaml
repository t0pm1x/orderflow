apiVersion: v1
kind: Service
metadata:
  name: {{ include "orderflow-inventory.fullname" . }}
  labels:
    {{- include "orderflow-inventory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "orderflow-inventory.selectorLabels" . | nindent 4 }}
