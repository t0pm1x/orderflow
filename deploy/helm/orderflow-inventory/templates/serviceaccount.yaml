{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "orderflow-inventory.serviceAccountName" . }}
  labels:
    {{- include "orderflow-inventory.labels" . | nindent 4 }}
{{- end }}
