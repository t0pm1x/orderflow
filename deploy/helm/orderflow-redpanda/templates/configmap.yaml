apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "orderflow-redpanda.fullname" . }}-config
  labels:
    {{- include "orderflow-redpanda.labels" . | nindent 4 }}
data:
  redpanda.yaml: |
    {{- toYaml .Values.config.redpanda | nindent 4 }}
