apiVersion: v2
name: orderflow-web
description: BFF + embedded SvelteKit SPA playground UI for orderflow (talks to order, payment, inventory over REST)
type: application
version: 1.1.7
appVersion: "v1.1.7-sveltekit"
