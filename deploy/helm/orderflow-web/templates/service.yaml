apiVersion: v1
kind: Service
metadata:
  name: {{ include "orderflow-web.fullname" . }}
  labels:
    {{- include "orderflow-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "orderflow-web.selectorLabels" . | nindent 4 }}
