{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "orderflow-web.serviceAccountName" . }}
  labels:
    {{- include "orderflow-web.labels" . | nindent 4 }}
{{- end }}
