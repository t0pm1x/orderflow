{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "orderflow-payment.serviceAccountName" . }}
  labels:
    {{- include "orderflow-payment.labels" . | nindent 4 }}
{{- end }}
