apiVersion: v1
kind: Service
metadata:
  name: {{ include "orderflow-saga.fullname" . }}
  labels:
    {{- include "orderflow-saga.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "orderflow-saga.selectorLabels" . | nindent 4 }}