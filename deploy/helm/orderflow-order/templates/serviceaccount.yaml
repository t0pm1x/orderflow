{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "orderflow-order.serviceAccountName" . }}
  labels:
    {{- include "orderflow-order.labels" . | nindent 4 }}
{{- end }}