apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "orderflow-redis.fullname" . }}-data
  labels:
    {{- include "orderflow-redis.labels" . | nindent 4 }}
spec:
  accessModes:
    {{- toYaml .Values.storage.accessModes | nindent 4 }}
  resources:
    requests:
      storage: {{ .Values.storage.size | quote }}
