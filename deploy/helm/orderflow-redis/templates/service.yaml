apiVersion: v1
kind: Service
metadata:
  name: {{ include "orderflow-redis.fullname" . }}
  labels:
    {{- include "orderflow-redis.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: redis
      port: {{ .Values.service.port }}
      targetPort: redis
      protocol: TCP
  selector:
    {{- include "orderflow-redis.selectorLabels" . | nindent 4 }}
